using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScoopSpawner : MonoBehaviour
{

    public Sprite[] scoopOutlines;
    public Material[] scoopFlavors;


    

    // Start is called before the first frame update
    void Start()
    {
        // if(len(scoopOutlines) == 0 || len(scoopFlavors) == 0) {
        //     Debug.LogError("No scoops or flavors for scoop spawner.");
        //     return;
        // }
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    private void ChooseScoop() {

    }
}
